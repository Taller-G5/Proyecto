server {
        listen 80;
        server_name localhost;
        
        location / {
            root /var/www/html;
            index index.html index.htm;
        }

        location /api/v1/user/ {
            proxy_pass http://user:5001/;
        }

}
